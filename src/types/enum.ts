export enum AccountStatus {
  Active = "active",
  Inactive = "inactive",
  Blocked = "blocked",
  Deleted = "deleted",
}

export enum Roles {
  Admin = "admin",
  HR = "hr",
  Employee = "employee",
}
