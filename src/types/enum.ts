export enum AccountStatus {
  Active = "active",
  Pending = "pending",
  Suspend = "suspend",
  Terminate = "terminate",
  Deleted = "deleted",
}

export enum Roles {
  Admin = "admin",
  HR = "hr",
  Employee = "employee",
}
